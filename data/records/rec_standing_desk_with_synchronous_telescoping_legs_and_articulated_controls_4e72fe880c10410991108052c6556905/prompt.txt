An L-shaped standing desk with three lifting columns, a corner desktop, and a front-mounted controller on the long run, plus a memory-control bank with three preset buttons and a small power button. the three columns remain visibly tied together by one rigid corner frame, and the control bank sits as a real pod beneath the front edge. The three lift stages slide prismatically inside their outer columns in sync, and the preset buttons and power button each depress independently as small prismatic push controls. Corner workstation scale.