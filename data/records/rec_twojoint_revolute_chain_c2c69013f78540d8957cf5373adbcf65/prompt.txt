An open-frame two-joint revolute chain. The root support is a simple pedestal, the links step down slightly in size toward the tip, and the end of the chain carries a compact end tab. The mechanism uses two serial revolute joints about parallel supported axes.