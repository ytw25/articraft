A two-leg office standing desk with a rectangular top, twin lifting columns, a crossbeam frame, and a handset under the front edge, plus a shallow keyboard tray under the front edge. the two inner lift stages are matched supports for the same desktop and stay aligned with one another, and the tray is mounted on short side guides below the top frame. Both lift stages slide prismatically within their outer leg columns as one synchronous height system, and the keyboard tray slides prismatically on its guides. Office desk scale.