A compact three-stage drawer-like slide. A fixed outer channel supports a middle runner and a smaller inner runner, with each stage retaining visible overlap. The assembly uses two serial prismatic joints through the nested stages.