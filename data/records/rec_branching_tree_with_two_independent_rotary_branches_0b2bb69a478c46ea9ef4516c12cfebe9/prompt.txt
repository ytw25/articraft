A rack-style two-branch rotary tree. A grounded upright spine carries two branch modules on separate side saddles, with each branch ending in a different simple hard-face such as a pad, fork, or clamp tab. Each branch is an independent revolute joint about its own supported hub axis.