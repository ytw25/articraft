A wall-backed two-joint revolute chain. A fixed backplate carries a root clevis and a sequence of articulated links, with enough spacing between links that each joint pair stays readable. The motion comes from two revolute joints in series on parallel axes.