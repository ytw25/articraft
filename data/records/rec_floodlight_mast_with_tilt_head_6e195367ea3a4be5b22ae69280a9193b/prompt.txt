A twin-head floodlight mast with a fixed pole on a weighted base plate and two independent lamp heads on separate side-extending arms at different heights. Each arm rotates on a horizontal revolute joint where it meets the mast; each lamp head tilts on a horizontal revolute joint at the arm tip.