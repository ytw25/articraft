A laptop stand with a low base frame, a single rigid top tray, and two telescoping side columns rising from the left and right rear corners. The two columns are matched supports for the same tray and remain aligned across the width of the stand. Each column extends prismatically along its vertical axis, and the tray is fixed rigidly across the tops of both columns.