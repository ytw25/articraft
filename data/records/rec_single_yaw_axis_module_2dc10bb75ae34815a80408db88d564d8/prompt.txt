A short-column yaw fixture. A fixed base supports a stub column and a compact rotating table whose diameter is clearly larger than the column below it. One revolute joint turns the table about the vertical column axis.