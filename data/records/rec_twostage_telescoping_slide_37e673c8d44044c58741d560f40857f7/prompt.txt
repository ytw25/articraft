A compact telescoping drawer slide. A fixed outer channel supports one moving inner section with a simple end plate, and the inner section reads as a captured linear member rather than a free bar. The inner section moves on a prismatic joint along the slide direction.