A two-branch positioning fixture. A rigid central spine carries an upper branch arm and a lower branch arm on separate side-supported hubs, with a small tooling pad at each tip. The two branches are independent revolute joints about parallel axes, and each joint should read as its own supported shaft and bearing set.