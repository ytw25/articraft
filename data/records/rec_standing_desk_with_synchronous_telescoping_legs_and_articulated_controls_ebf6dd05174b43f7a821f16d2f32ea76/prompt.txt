A two-leg office standing desk with a rectangular top, twin lifting columns, a crossbeam frame, and a handset under the front edge, plus a paddle-style controller with separate up and down paddles. the two inner lift stages are matched supports for the same desktop and stay aligned with one another, and the two paddles remain visibly split instead of reading as one rocker strip. Both lift stages slide prismatically within their outer leg columns as one synchronous height system, and the up and down paddles rotate on short local pivots in the controller body. Office desk scale.