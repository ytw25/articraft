A low-profile two-stage extension slide. A broad fixed outer section supports a smaller inner runner with clear stop hardware and a square front face. The inner runner is one prismatic joint along the shared axis.