An L-shaped standing desk with three lifting columns, a corner desktop, and a front-mounted controller on the long run, plus a paddle-style controller with separate up and down paddles. the three columns remain visibly tied together by one rigid corner frame, and the two paddles remain visibly split instead of reading as one rocker strip. The three lift stages slide prismatically inside their outer columns in sync, and the up and down paddles rotate on short local pivots in the controller body. Corner workstation scale.