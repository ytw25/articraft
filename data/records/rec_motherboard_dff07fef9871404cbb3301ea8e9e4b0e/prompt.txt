A compact mini-ITX motherboard with one main expansion slot and two memory slots. The flat populated board includes a CPU socket retention arm, small slot latches, and a connector-release clip near the board edge. The socket arm rotates on its hinge pin at the socket frame, and each latch or retention clip rotates on its own local revolute pivot at the connector it serves.