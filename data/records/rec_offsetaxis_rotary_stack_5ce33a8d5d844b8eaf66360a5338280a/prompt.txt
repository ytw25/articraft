An offset-axis rotary stack. A grounded base turntable carries a stiff side arm that supports a second rotary stage on a parallel but laterally offset axis, topped by a small flange or faceplate. The base stage and the upper stage are separate revolute joints about their own supported parallel axes.