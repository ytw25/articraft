An offset-axis rotary stack. A base turntable carries a raised side arm that supports a second rotary stage on a parallel but laterally offset axis, topped by a small flange. The base stage rotates about the main vertical axis and the upper stage rotates about the offset parallel axis, each roughly 120 degrees each way, with the base allowed to go continuous if needed.