A bench-style standing desk with a long top, three lifting columns, and a front paddle controller beneath the center edge, plus a paddle-style controller with separate up and down paddles. the three telescoping columns clearly support the same rigid desktop frame, and the two paddles remain visibly split instead of reading as one rocker strip. All three inner leg stages slide prismatically inside their outer columns in sync, and the up and down paddles rotate on short local pivots in the controller body. Shared-workstation desk scale.