A fork-root elbow module. A fixed fork captures the upper arm root, and the upper arm carries the forearm to a plain end plate. The root fork supports the shoulder revolute and the upper arm supports the elbow revolute.