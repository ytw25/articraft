A saddle-body offset stack. A broad lower stage supports a side-mounted support cheek that carries a compact secondary rotary output. The lower stage rotates on one axis and the upper output rotates on an offset parallel axis.